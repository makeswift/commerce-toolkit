## How the data-slot attribute is used

## How Tailwind classes are organized

## Why data attributes are used for styling

## How icons are rendered

Anywhere I'm passing in an Lucide Icon should be reconsidered switching to a component that uses the `asChild` prop, that way the styles would also be passed to the children if the user decides to incorporate their own icon library.

## How images are rendered

## How links are rendered

## When to use 'use-client'

## When to use React context

## How to extend the `asChild` prop to the monolith component interface
